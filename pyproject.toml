[project]
name = "periforge"
version = "0.1.0"

[project.scripts]
periforge = "main:generate"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["main"]